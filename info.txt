 

// Фильтрация отзывов на основе выбранных дат
let filteredReviews = reviews.filter((review) => {
  const reviewDate = new Date(review.timestamp);
  const startDateValue = startDate ? new Date(startDate) : null;
  const endDateValue = endDate ? new Date(endDate) : null;

  // Проверка, попадает ли дата отзыва в выбранный диапазон
  if (startDateValue && endDateValue) {
    return reviewDate >= startDateValue && reviewDate <= endDateValue;
  } else if (startDateValue) {
    return reviewDate >= startDateValue;
  } else if (endDateValue) {
    return reviewDate <= endDateValue;
  }

  return true;
});

// Сортировка отфильтрованных отзывов по дате (timestamp) сначала новые
filteredReviews.sort((a, b) => {
  const dateA = new Date(a.timestamp);
  const dateB = new Date(b.timestamp);
  return dateB - dateA;
});






{filteredReviews.map((review) => {
  return (
    <UserReviewForm
      key={review.id}
      review={review}
      handleDelete={() => handleDelete(review.id)}
    />
  );
})}



function Reviews({ reviewsProps }) {
  const initialState = { name: "", comment: "", rating: 5, avatar: "woman" };
  
  // Остальной код компонента...
  
  const [filteredReviews, setFilteredReviews] = useState(reviewsProps);
  const [startDate, setStartDate] = useState("");
  const [endDate, setEndDate] = useState("");
  
  // Фильтрация отзывов на основе выбранных дат
  useEffect(() => {
    let filteredReviews = reviewsProps.filter((review) => {
      const reviewDate = new Date(review.timestamp);
      const startDateValue = startDate ? new Date(startDate) : null;
      const endDateValue = endDate ? new Date(endDate) : null;

      if (startDateValue && endDateValue) {
        return reviewDate >= startDateValue && reviewDate <= endDateValue;
      } else if (startDateValue) {
        return reviewDate >= startDateValue;
      } else if (endDateValue) {
        return reviewDate <= endDateValue;
      }

      return true;
    });

    // Сортировка отфильтрованных отзывов по дате (timestamp) сначала новые
    filteredReviews.sort((a, b) => {
      const dateA = new Date(a.timestamp);
      const dateB = new Date(b.timestamp);
      return dateB - dateA;
    });

    setFilteredReviews(filteredReviews);
  }, [reviewsProps, startDate, endDate]);

  // Остальной код компонента...
}

ssh -R 80:localhost:3000 serveo.net